apiVersion: apps/v1
kind: Deployment
metadata:
  name: habitquest-redis
  labels:
    db: habitquest-redis
spec:
  selector:
    matchLabels:
      db: habitquest-redis
  template:
    metadata:
      labels:
        db: habitquest-redis
    spec:
      containers:
        - name: habitquest-redis
          image: redis:7.2
          resources:
            requests:
              cpu: 100m
              memory: 50Mi
            limits:
              cpu: 200m
              memory: 100Mi

---

apiVersion: v1
kind: Service
metadata:
  name: habitquest-redis
  labels:
    db: habitquest-redis
spec:
  type: ClusterIP
  selector:
    db: habitquest-redis
  ports:
  - protocol: TCP
    port: 6379
    targetPort: 6379
