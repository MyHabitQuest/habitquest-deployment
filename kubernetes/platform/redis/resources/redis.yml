apiVersion: apps/v1
kind: Deployment
metadata:
  name: crewcash-redis
  labels:
    db: crewcash-redis
spec:
  selector:
    matchLabels:
      db: crewcash-redis
  template:
    metadata:
      labels:
        db: crewcash-redis
    spec:
      containers:
        - name: crewcash-redis
          image: redis:7.2
          resources:
            requests:
              cpu: 100m
              memory: 50Mi
            limits:
              cpu: 200m
              memory: 100Mi

---

apiVersion: v1
kind: Service
metadata:
  name: crewcash-redis
  labels:
    db: crewcash-redis
spec:
  type: ClusterIP
  selector:
    db: crewcash-redis
  ports:
  - protocol: TCP
    port: 6379
    targetPort: 6379
